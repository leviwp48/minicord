version: '3'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ${PWD}/app:/app
      # - ./app:/app
    ports:
      - "80:80"
    environment:
      - NUM_PROCS=2
      - API_PROCS=144
      - CRON_PROCS=0
      - APP_ENV=levi-test
      - SUPERVISOR_APP_STOPWAITSECS=20
      - SECRET_KEY=big-secret
      - FLASK_DEBUG=1
      - ACCOUNT_SID=AC06a51df77203c3010726688868097d65
      - API_KEY=SK07e9015c5a0d44929b55a575bd91fe73
      - API_SECRET=jeUEBvO8VCnaQPI8lJP9kWvECCQB99iK
